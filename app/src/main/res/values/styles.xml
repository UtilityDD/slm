<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base style for the new full-width menu cards -->
    <style name="Widget.App.MenuCard.FullWidth" parent="Widget.Material3.CardView.Outlined">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="cardBackgroundColor">@color/card_bg_light</item>
        <item name="strokeColor">@color/card_stroke_light</item>
        <item name="strokeWidth">0.8dp</item>
    </style>

    <!-- Style for the horizontal LinearLayout inside the new cards -->
    <style name="Widget.App.MenuCard.LinearLayout.Horizontal" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- Style for the ImageView in the new horizontal layout -->
    <style name="Widget.App.MenuCard.ImageView.Horizontal" parent="">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_marginEnd">16dp</item>
    </style>

    <!-- Style for the TextView in the new horizontal layout -->
    <style name="Widget.App.MenuCard.TextView.Horizontal" parent="">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAppearance">?attr/textAppearanceTitleMedium</item>
        <item name="android:textColor">@color/title_text</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>